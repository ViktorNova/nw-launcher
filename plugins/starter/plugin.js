    plugins.register(function() {
        /**
         * Initializes the plugin.
         * This function may register timeouts callbacks, create data structures needed for the other functions to work
         */
        this.initialize = function () {

        },
        /**
         * Returns a number of json objects found by the search string
         * @param searchString
         */
        this.search =function (searchString) {

        }
    };

})();